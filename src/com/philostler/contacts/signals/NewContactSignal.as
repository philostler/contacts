package com.philostler.contacts.signals {
	import org.osflash.signals.Signal;
	
	/**
	 * Signal representing the intention of displaying the UI for a new contact to be entered
	 */
	public class NewContactSignal extends Signal {
		public function NewContactSignal() {
			super();
		}
	}
}